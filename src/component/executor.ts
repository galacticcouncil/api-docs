import {LitElement, html, css} from 'lit';
import {customElement, property, state} from 'lit/decorators.js';

@customElement('ui-executor')
export class Executor extends LitElement {
  static styles = css``;

  render() {
    return html` <div></div>`;
  }
}
